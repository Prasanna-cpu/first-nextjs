'use client';
import React from 'react'

const AddtoCart = () => {
  return (
    <div>
        <button className='btn btn-primary' data-theme='winter' onClick={()=>{console.log('Click')}}>Add to Cart</button>
    </div>
  )
}

export default AddtoCart